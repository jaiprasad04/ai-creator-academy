# Changelog

## 2026-07-21

- Initial repo scaffolding: `README.md`, `ROADMAP.md`, `LESSON_TEMPLATE.md`, `CONTRIBUTING.md`.
- Track 1 (AI Video Ads & UGC) written in full — 5 modules, each with `module.md`, `outputs/`, and `app.json`.
- Tracks 2-6 stubbed with a `README.md` each, marked coming soon in `ROADMAP.md`.
- Flattened Track 1's layout: each module is now a single `<NN>-<slug>.md` file directly under the track folder (no per-module subfolder), with `outputs/` and `app.json` files consolidated per-track — fewer clicks to read a module on GitHub.
- Consolidated Track 1's five separate `<slug>.app.json` files into one shared `apps.json` per track, keyed by module slug — same info, one less file per module.
- Added prev/next navigation links to the bottom of every Track 1 module.
